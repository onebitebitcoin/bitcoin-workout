# expand/contract 배포용 — 이 파일이 있으면 scripts/deploy.sh 가 마이그레이션을 두 번에 나눠 올린다.
#
#   Step 4 (구 슬롯이 아직 트래픽을 받는 중): 아래 리비전까지만
#   Step 7 (구 슬롯 종료 후):                  head 까지
#
# 왜 나누나
#   두 슬롯은 같은 DB 를 본다. 구 코드가 쓰던 테이블·컬럼을 구 슬롯이 살아있는 동안
#   없애면, nginx 가 아직 트래픽을 보내는 그 슬롯이 사라진 것을 조회하다 500 을 낸다.
#   업로드는 ffmpeg 처리를 다 끝낸 뒤 마지막 저장에서 깨져 사용자가 결과물을 잃는다.
#
# 여기 적을 값
#   구 코드가 봐도 안전한 마지막 리비전. 보통 ADD COLUMN·데이터 이동까지다.
#   그 다음(DROP TABLE/COLUMN 등)은 자동으로 contract 단계로 밀린다.
#
# 이번 배포(v0.20.0)의 체인
#   d953acd3a18d  (운영 현재)
#   → 004aa221ea8d  카테고리 리맵      데이터만 바꾼다. 구 코드 안전
#   → cbb7d21fb47c  ADD btc_price_krw  구 코드는 이 컬럼을 모른다. 안전  ← 여기까지 expand
#   → c7d8e9f0a1b2  DROP reward_points 구 코드가 8개 파일에서 참조한다. contract
#
# 배포가 끝나면 이 파일을 지운다. 파일이 없으면 지금까지처럼 head 까지 한 번에 올린다.

cbb7d21fb47c
