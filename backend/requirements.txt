fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.49
alembic==1.14.0
pydantic==2.10.6
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.20
boto3==1.35.80
psycopg2-binary==2.9.10
httpx==0.28.0
bech32==1.2.0
redis==5.0.8
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
Pillow==11.2.1
email-validator==2.3.0
ruff>=0.15.0
opencv-python-headless==4.10.0.84
numpy>=1.26
# mediapipe(운동열 필터, app/services/muscle_heat.py)의 실제 의존성.
# mediapipe 패키지 자체는 여기 넣지 않는다 — 정상 설치 시 opencv-contrib-python(GUI 포함)을
# 끌어와 opencv-python-headless의 cv2를 덮어써 버전이 꼬인다. `pip install --no-deps mediapipe`로
# 별도 설치한다(scripts/install.sh, Dockerfile 참고).
absl-py
flatbuffers
matplotlib
sounddevice
